# 付费股票报告系统：中文文档入口

这套系统把现有 TradingAgents 分析能力包装成一次性付费产品：用户选择美股代码和报告语言，在 Creem 收银台付款，系统在后台生成并保存报告，再把报告链接发到付款邮箱。

原来的英文说明已整理为下面两份中文文档。文件名 `PAID_REPORT_MVP.md` 保留，已有链接仍然可用；`.md` 是 Markdown 文档格式。

| 你想了解什么 | 阅读文档 |
| --- | --- |
| 系统有哪些模块，支付后如何执行，报告存在哪里，为什么不会重复跑任务 | [付费报告系统架构](付费报告系统架构.md) |
| 如何安装、启动、填写配置、部署到服务器、查询订单、导出报告、备份恢复及排查故障 | [付费报告部署与运行](付费报告部署与运行.md) |
| 如何启用 FMP、选择 Marketstack 等数据源和回滚，如何验证真实接口 | [数据源配置](付费报告部署与运行.md#34-fmp-数据源配置与回滚) · [真实接口验收](付费报告部署与运行.md#113-fmp-真实接口验收) |
| 行情、指标、公司身份及购买校验如何共用适配层 | [行情与证券资料适配](付费报告系统架构.md#31-行情与证券资料适配) |
| 第五位行业分析师做什么、如何传递证据与控制范围 | [行业与产业链分析](付费报告系统架构.md#33-行业与产业链分析) |
| SEC 如何配置、行业来源如何选择、哪些数据已实测可用 | [行业配置](付费报告部署与运行.md#35-行业与产业链分析配置) · [实际验证记录](行业分析数据源接入验证.md) |

## 目前能做什么

- 首页选择美股代码及 11 种报告语言。
- 单一一次性产品，价格、币种和税费方式从 Creem 读取；用户无需注册或登录。
- 通过服务端验证过的支付通知启动后台分析，浏览器跳转不能充当支付凭证。
- 报告正文同时保存到 Markdown 文件和 SQLite 数据库，并与订单关联。
- 用随机私密链接查看报告，用命令行离线查询订单、导出报告和备份数据库。
- 保存邮件发送记录，处理重复支付通知、分析失败、任务中断和邮件重试。
- 新股票订单默认使用市场、情绪、新闻、基本面、行业与产业链五位分析师；行业报告进入多空与风控上下文、完整报告和独立文件。ETF/加密资产不运行行业角色，旧订单保存的角色列表不自动追加。
- 付费入口默认使用 FMP 美股日线、证券资料、基本面、财报、新闻和内部人交易，指标在本地计算；本地 CLI/Web 默认使用 yfinance。ETF 报告能力已保留，但 `TRADINGAGENTS_ETF_REPORTS_ENABLED=false` 默认暂停新 ETF 购买；开启后支持名单内的 SPY、QQQ、VOO、IVV、VTI、DIA、IWM，基金资料和持仓权限仍需验证。

## 你当前可以先做的事

先在同一个 `.env` 中填写 `CREEM_TEST_*`、`CREEM_PROD_*` 两组 Creem 配置，以及共用的网站、邮件、人机验证和模型配置。只需填齐本次启动模式的 Creem 凭据；缺少必填项会直接拒绝启动并列出变量名。付费入口默认使用 FMP，另需填写 `FMP_API_KEY` 并确认接口权限；保持五个供应商选择器未设置即可让本地 CLI/Web 继续默认使用 Yahoo。显式覆盖方式见[数据源配置](付费报告部署与运行.md#34-fmp-数据源配置与回滚)，供应商不随 Creem 的测试/正式模式切换。

在当前电脑的项目目录中运行：

```sh
cd /Users/chengshuo/PycharmProjects/TradingAgents
COMMERCE_PUBLIC_URL=http://localhost:8000 \
.venv/bin/tradingagents-mvp serve --mode test
```

然后打开 [本地首页](http://localhost:8000)。正式环境使用 `serve --mode prod` 并配置 HTTPS 网站地址；不传 `--mode` 时读取 `CREEM_MODE`，缺省为 `test`。按 `Ctrl+C` 停止。若是新机器或尚未安装依赖，先按[部署与运行文档](付费报告部署与运行.md)完成安装。

行业分析的 SEC 来源无需 API Key，优先使用 `SEC_USER_AGENT`，未配置时复用本项目已授权的 `COMMERCE_SUPPORT_EMAIL`；FRED 使用 `FRED_API_KEY`。行业来源独立于原有五个供应商选择器：把行情改为 Yahoo 不会关闭行业 FMP 采集。先运行[行业来源验收](付费报告部署与运行.md#114-行业来源与报告验收)，再在部署环境检查完整报告。缺少某个来源会明确降级，不把已取得公司 profile 等同于全部行业数据可用。

代码已完成本地验证；真实支付、真实模型运行和邮件收件的完整联调仍需使用你实际配置的服务完成。本地测试通过，不代表域名、商户账户或发信域名已配置成功。

FMP 真实接口烟测仍需具有相应权限的 Key；缺少 Key 时付费网站不能通过股票资格校验。Marketstack 可继续通过配置用于行情和资料。FMP 数据商业展示授权与 SDK 代码许可不同，正式售卖前需按实际合同确认。
